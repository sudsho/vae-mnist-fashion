# 2D latent for the latent-grid traversal viz

data:
  root: ./data
  batch_size: 128
  num_workers: 2

model:
  arch: mlp
  input_dim: 784
  hidden_dim: 400
  latent_dim: 2

train:
  epochs: 30
  lr: 0.001
  beta: 1.0
  seed: 42

log:
  out_dir: ./runs/latent2d
  save_every: 5
